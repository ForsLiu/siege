# BACKLOG.md — main lane (ordered; execute top-down)

Format: `- [ ] (id) [infra|feat|bug|balance|polish] title — acceptance: <objective check> — refs: <SPEC § | infra>`
P0 is content-agnostic engine and tooling. P1+ are written by SPEC intake when SPEC.md lands (see CLAUDE.md). Tick items `[x]` when done; never delete them.

Bootstrap (2026-09-05) built the skeleton every P0 item extends: sim (rng, hash, hex, stats, effects v0, fight, run, commands, replay), data pipeline (zod, loader, content hash, dev endpoint), tools (fight, sim, sweep, bench, `random` policy), renderer + app shell, tests. Item texts below say what already exists.

**Order is fixed by the owner triage verdict of 2026-09-05 (feedback/processed/2026-09-05-07-triage-verdict.md, QUESTIONS.md "Owner overrides"): owner-request items outrank QA-filed bugs; P0-B4/B5/B6 are deferred to the end of P0. Execute top-down; do not re-sort.**

## P0 — engine & tooling

### Done

- [x] (P0-B1) [bug] `tools/sweep.ts` workers crash on Node >= 22.18 — the worker loads `src/data/loader.ts` through Node's strip-only TypeScript mode, which rejects the `ContentError` parameter properties (`ERR_UNSUPPORTED_TYPESCRIPT_SYNTAX`), so every sweep run dies before producing metrics. — acceptance: a failing regression test first; then `npx tsx tools/sweep.ts --seeds 10 --policies random` completes with 0 exceptions on this Node. — refs: infra
- [x] (P0-B2) [bug] Sweep robustness (QA on P0-B1) — a crashed worker aborts the whole sweep and discards every completed result (`worker exited with N`, no report written); `--out` into a missing directory or onto a directory path throws ENOENT/EISDIR *after* all the work; `SIEGE_SWEEP_WORKERS` bypasses the 1..64 validation the `--workers` flag enforces (`-5` silently runs serial, `99999` spawns 99999 threads); duplicate `--policies` entries double-count. — acceptance: tests in tests/tools.test.ts for each: a worker that exits non-zero leaves the surviving results and marks the dead job `ok:false`; `--out` into a nested missing dir either creates it or fails before any job runs; the env worker count is validated exactly like the flag; duplicates are de-duplicated. — refs: infra
- [x] (P0-B3) [bug] An invalid `SIEGE_TEST_WORKERS` silently runs zero tests and exits 0 (QA on P0-B2) — `SIEGE_TEST_WORKERS=-5 npm run test:fast` prints no "Test Files" line and returns 0, because `vitest.config.ts` passes the value straight to `maxWorkers` and its `|| 4` guard catches `NaN` and `0` but not a negative. The per-item gate can therefore report green having run nothing. — acceptance: a failing regression test first; `resolveTestWorkers(env)` exported from the config and unit-tested (`-5`, `0`, `abc`, `''`, `' 4 '` all yield a count >= 1 or a usage error), plus a `spawnSync` test proving `SIEGE_TEST_WORKERS=-5` either fails loudly or still collects test files. — refs: infra (QA on P0-B2)
- [x] (P0-01) [infra] Effect vocabulary v1 — extend the interpreter (v0 has damage/heal/statMod/stun, targets self|target|allies|enemies, hooks onCombatStart/onAttack/onHit/onCast/onKill/onDeath) to: damage(kind, amount, scaling by stat), heal, shield(amount, duration), statMod(stat, mul|flat, duration), stun(duration), applyTag(tag, duration), spawnProjectile(ref); triggers onCombatStart, onRoundStart, onAttack, onHit, onCast, onKill, onDeath, onTakeDamage, aura(range, effects); damage pipeline with ordered hooks (pre-mitigation → mitigation → post) and a per-fight damage ledger per unit (a basic dealt/taken/healed ledger exists). Dev sample units in data/dev exercise every effect. — acceptance: each effect has a fast test proving its numeric result; an unknown effect name fails schema validation; determinism suite green. — refs: infra

### 1. Owner playtest round 1 — dev tooling, UI shell and run HUD (verdict order)

Owner-request items (refs: owner-req) outrank every QA-filed bug. `P0-09` sits third by the owner's verdict: the UI shell hardening it does (drag placement, letterboxing, an extracted run controller) is what the inspector, HUD and sandbox screens build on. See QUESTIONS.md FB-01…FB-04 for the earlier gating decisions.

- [x] (P0-15) [feat] `dev:` command namespace — every dev cheat is a sim `Command` in a `dev:` namespace recorded in the command log: `dev:gold`, `dev:xp`, `dev:invinciblePieces`, `dev:invinciblePlayer`, `dev:skipRound` (resolve the round as a win with normal rewards), `dev:addAugment`, `dev:openShop` (free reroll into a chosen tier), `dev:spawnUnit` (id, star, bench or hex), `dev:giveItem`. Acceptance of `dev:` commands is a `RunConfig` flag that only dev builds set. — acceptance: a fast test per command proving its state effect; every `dev:` command is rejected with a reason when the flag is off and accepted when on; a run whose log contains `dev:` commands replays with every round hash matching. — refs: owner-req (feedback/2026-09-05-01-dev-modes.md §A)
- [x] (P0-16) [feat] Dev panel (F2) — dev-build-only panel with a button per P0-15 command, showing the run seed and the content hash for bug reports; loaded through the existing dev chunk so production carries none of it. — acceptance: F2 toggles the panel; each button dispatches its Command through the normal dispatch path; `tests/slow/build.test.ts` extended to prove `dist/` contains no dev panel; manual checklist per button in PROGRESS.md. — refs: owner-req (feedback/2026-09-05-01-dev-modes.md §A)
- [x] (P0-09) [infra] UI shell hardening — resolution-independent scaling (letterbox 16:9), mouse-drag placement with snap and invalid-drop feedback (click-to-place exists), hotkeys (reroll, buy xp, sell, start exist as R/X/S/Space; verify and document), pause menu with abandon run (exists), results screen with run summary (basic version exists); screen state machine fully unit-tested (tests/screens.test.ts covers every transition today; keep it complete); extract the run controller (dispatch / playback / abandon) out of `src/app/main.ts` so it is unit-testable, with a regression test that `abandon` issued during fight playback ends the run (QA bootstrap bug 2, fixed in main.ts without a test); manual checklist in PROGRESS.md. — acceptance: state-machine tests cover every transition; checklist ticked. — refs: infra
- [x] (P0-17) [feat] Battle sandbox core — `SandboxSetup` (units per side with star, items and per-stat overrides from `STAT_KIND`, plus fight-rule overrides: max seconds, overtime on/off) and `runSandbox(setup, seed, n)` returning win rate per side, mean and p95 fight length, per-unit damage dealt / taken / healed and deaths. Sandbox state never touches run state, save files or telemetry. — acceptance: aggregation over a fixture equals a direct loop of `fight()` calls with the same seeds; save → load of a setup through `data/dev/boards/<name>.json` round-trips identically; a test proves no sandbox function reads or writes `RunState`. — refs: owner-req (feedback/2026-09-05-01-dev-modes.md §B)
- [x] (P0-18) [feat] Battle sandbox screen — Title → Sandbox: roster list, place any unit on either side, edit star / items / stat overrides / fight rules, "Run once" visualised through the normal renderer at 1/2/4× with a "replay this fight" button (same seed), "Run N times" (default 50) headless with the P0-17 results table, and save / load a setup through the dev data endpoint. — acceptance: screen-state tests cover every Sandbox transition (enter, run once, replay, run N, save, load, exit); manual checklist in PROGRESS.md. — refs: owner-req (feedback/2026-09-05-01-dev-modes.md §B)
- [x] (P0-19) [feat] Unit inspector model — pure `inspectorModel(state, uid)` in `src/app`: name, tier/cost, star, traits with a one-line effect each, ability name and text with the current numbers filled in, every stat as current + base + the list of modifier sources (trait, item, augment, ability, star), items equipped, mana current/max, and in combat the live values (hp, mana, shields, active stat mods). Selection lives outside sim state. — acceptance: the model lists every modifier source that contributes to a stat and the current value equals the base combined with exactly those sources under the stacking rule; a test proves setting or changing the selection leaves sim state and every hash unchanged. — refs: owner-req (feedback/2026-09-05-02-unit-inspector.md)
- [x] (P0-20) [feat] Inspector panel & board readability — clicking a piece (board, bench, shop card, sandbox) opens the P0-19 panel; empty space or Esc closes it. Renderer: player half and enemy half shaded differently at all times, hover highlight on the hex under the cursor, and an attack-range hex ring (plus the ability range when the ability has one) for the selected unit. — acceptance: a renderer test proves the ring cells equal `hexesWithin(origin, range)` on three sample units and that the shading covers exactly the rows `data/board.json` assigns to each half; manual checklist: shop, bench, board and sandbox pieces all open the panel. — refs: owner-req (feedback/2026-09-05-02-unit-inspector.md)
- [x] (P0-21) [feat] Income preview, gold panel & round-end summary — the sim exposes `incomePreview(state)` returning the breakdown for the coming round (base income, interest with the rule it applied, win/loss streak bonus where the rules define one, encounter reward gold); the run HUD gold panel and the round-end summary both render that breakdown, and the summary also shows the result, hp lost, rewards line by line and the next round's encounter type. — acceptance: `incomePreview(state).total` equals the gold actually granted at `nextRound` for 50 random-policy runs; a test asserts the breakdown parts sum to the total; the UI reads the breakdown from the sim and computes none of it. — refs: owner-req (feedback/2026-09-05-03-run-hud.md §2, §5)
- [ ] (P0-22) [feat] Round track — top bar listing every round of the run in order with an icon per encounter type from data (normal, elite, boss, treasure/loot, augment), the current round marked and past rounds dimmed; hovering a round shows its reward preview (gold, xp, item drops, augment offer) from the encounter data. — acceptance: the encounter schema carries `type` and a reward preview, and a test proves the derived track matches the encounter list order and is identical across a replay of the same seed. — refs: owner-req (feedback/2026-09-05-03-run-hud.md §1)
- [ ] (P0-23) [feat] Shop HUD — tier colours on shop cards, the per-tier odds for the current level (from `economy.shopOdds`) next to the level indicator, an xp progress bar, and the reroll and buy-xp costs on their buttons. — acceptance: a shop view-model test proves the rendered odds row equals `economy.shopOdds[level]` and the costs equal the rules values for three levels; no tuning number appears in `src/ui`. — refs: owner-req (feedback/2026-09-05-03-run-hud.md §3)
- [ ] (P0-24) [feat] Augments — augment rows in `data/dev` (id, name, effect text, effects using the vocabulary), offers drawn from the `augment` RNG stream on data-defined augment rounds, a `pickAugment` Command, and chosen augments shown as icons with tooltips. — acceptance: offers are deterministic per seed (same seed → same N options); a numeric test proves the picked augment's effects apply in the next combat; the schema rejects an unknown augment id; picking outside an augment round is rejected with a reason. — refs: owner-req (feedback/2026-09-05-03-run-hud.md §4)

### 2. Owner playtest round 1 — traits, items and combat FX

Ordered traits → items → combat FX; the FX item needs P0-02's projectile model for trails (QUESTIONS.md FB-03). All content is `data/dev` samples (FB-02).

- [ ] (P0-25) [feat] Trait system — a trait is a data row (id, name, description, `breakpoints: [{count, effects[]}]` using the effect vocabulary, `teamWide: true|false`); unit rows list their traits; the active count is the number of *unique unit ids* on the board carrying the trait (duplicates and higher stars count once); breakpoint effects apply at combat start through the existing hook bus and an inactive trait applies nothing. — acceptance: tests for unique-unit counting (a duplicate and a 2-star copy each add nothing), activation exactly at the breakpoint count and deactivation one below, and `teamWide` applying to all allies vs holders-only; the schema rejects a unit referencing an unknown trait. — refs: owner-req (feedback/2026-09-05-05-traits.md)
- [ ] (P0-26) [feat] Trait panel — run-screen panel listing every trait on the board sorted by active tier then count, each with icon/name and count vs breakpoints ("3 / 2 4 6", reached breakpoint highlighted); hover shows the effect text per breakpoint and the contributing units. Traits also appear in the unit inspector and on shop cards. — acceptance: a pure view-model test over a fixture board proves the sort order, the counts and the highlighted breakpoint; trait effects show up as modifier sources in the P0-19 inspector model; manual checklist: the panel updates live as units are placed and benched. — refs: owner-req (feedback/2026-09-05-05-traits.md)
- [ ] (P0-27) [feat] Items core — item rows (id, name, `kind: component | completed`, stat mods, effects) plus a recipe table mapping `[componentA, componentB] → completed` (order-independent; two of the same component is a valid recipe); up to 3 items per unit; an item bench; `equipItem` Command validated for slot cap and target; placing a component on a unit already holding a component combines them immediately through the recipe table, and a completed item occupies one slot. — acceptance: combining tests (order independence, same-component recipe, an unknown pair leaves two components); the 3-item cap rejects with a reason; item stat mods appear as one modifier source per item and stack per the stat-stacking rule. — refs: owner-req (feedback/2026-09-05-06-items.md)
- [ ] (P0-28) [feat] Item lifecycle & loot — selling a unit returns its items to the bench; merging three copies moves the items onto the resulting unit up to the cap with the overflow returning to the bench; encounters drop items through data-defined loot tables rolled on the `loot` RNG stream (component drops, choice-of-N drops, completed-item drops), shown in the round-end reward summary and the P0-22 round-track preview. — acceptance: sell-returns and merge-transfer-with-overflow tests (including a merge of units carrying 3 items each); loot rolls are identical for the same seed and change with the seed; a choice-of-N drop is a Command; loot appears in the reward summary. — refs: owner-req (feedback/2026-09-05-06-items.md)
- [ ] (P0-29) [feat] Item bench UI — drag an item from the bench onto a unit (equip), drag a component onto a unit already holding a component to complete it, invalid-drop feedback with the sim's rejection reason; works on the run screen and in the sandbox. No un-equip Command (a removal hook is left for the consumable SPEC will define). — acceptance: drag-controller unit tests for a legal equip, a full-slot rejection and a drop on empty space; manual checklist: component onto a unit holding a component shows the completed item, in run and in sandbox. — refs: owner-req (feedback/2026-09-05-06-items.md)
- [ ] (P0-30) [feat] Combat FX — per-event visuals driven only by sim events: attack (melee swing arc, or projectile trail plus impact flash for ranged), spell cast (unit flash + short ability-name toast), shield gained (shield segment on the hp bar + shimmer), heal (green pulse), lifesteal (green pulse on the attacker tied to the hit); a dev toggle disables all FX. — acceptance: a replay produces identical hashes with FX enabled and disabled; each of the five effect types has a sandbox fixture that triggers it, ticked in the manual checklist. — refs: owner-req (feedback/2026-09-05-04-combat-fx.md §1)
- [ ] (P0-31) [feat] Floating damage numbers — a number on every damage instance, coloured by damage kind from `data/ui/theme.json` (physical, magic, true, and a fourth colour for damage-over-time ticks, which are a `dot` delivery flag on damage events, not a fifth kind — smaller, no impact flash); heals and lifesteal as green "+n"; size = base + k·log10(1 + amount) clamped to a min and max; numbers on the same unit in the same tick merge into one; pooled objects and a dev FX toggle. — acceptance: the theme schema requires a colour for every damage kind and for the dot flag; a merging test (two hits on one unit in one tick → one number with the sum); a pool test proves no allocation after warm-up; the fps overlay holds the dev budget at 4× with 20 units per side. — refs: owner-req (feedback/2026-09-05-04-combat-fx.md §2, §3)

### 3. Remaining engine & tooling

Two money-path bugs QA found while exercising P0-20's manual checklist (CLAUDE.md triage rule: breaks a
money path -> top of queue), both pre-existing in the P0-09 drag/click and resize wiring, not in P0-20's
own code. Owner-request items above (P0-21…P0-31) still outrank them per the triage rule, so they sit
here, above the rest of this section.

- [ ] (P0-B11) [bug] Canvas hit-testing uses stale geometry until a real window resize fires (QA on P0-20) — `BoardRenderer.resize()` runs from `syncScreens()` (`src/app/main.ts`) before `refreshUi()` populates the screen's shop/bench/HUD DOM; `#app`'s CSS grid (`1fr auto`) is still oversized at that moment, so `resize()` bakes in too-large dimensions for both drawing and `cellAt()` hit-testing. Once the real content lays out a moment later the canvas's actual CSS box shrinks, but the renderer's internal geometry is never recomputed until an unrelated `resize` event fires — reproduced entering both the run and sandbox screens (internal vs. CSS canvas size mismatched by ~20%), breaking click-to-select/place/sell until the player happens to resize the window. — acceptance: a failing regression test first (a browser-driven or `ResizeObserver`-mockable test proving `cellAt` stays correct immediately after entering the run/sandbox screen, with no window resize); fixed by resizing after `refreshUi()` or driving `resize()` from a `ResizeObserver` on the canvas; note in QUESTIONS.md if a Playwright/browser test harness needs to be added first (none exists yet — `[infra]` prerequisite). — refs: infra (QA on P0-20)
- [ ] (P0-B12) [bug] `suppressClick` sticks `true` after a bench-to-board drag, silently eating the next board click (QA on P0-20) — `endDrag` (`src/app/main.ts`) sets `suppressClick = true` on every completed drag to swallow the browser's trailing native `click`, which only fires on the drag's own `mousedown` target. A board-piece drag's `mousedown`/`mouseup`/native-`click` all target the canvas, so the flag clears correctly; a bench-card-initiated drag's `mousedown` targets the bench card while the drop's `mouseup` resolves against the canvas, so no native `click` ever fires on canvas to clear the flag — the very next real board click is silently dropped (confirmed 2/2: buy a unit, drag it from the bench onto the board, then click anywhere on the board — nothing happens; an identical second click works). — acceptance: a failing regression test first, proving a bench-origin drag followed by a canvas click both still register (only settable if `suppressClick`'s state is extracted somewhere testable, e.g. tracked by the drag's originating target rather than a bare flag); fixed by only suppressing when the drag's `mousedown` originated on the canvas, or by clearing `suppressClick` on the next `mousedown` instead of relying on a `click` that may never arrive. — refs: infra (QA on P0-20)

- [ ] (P0-02) [infra] Attack types & projectiles — melee vs ranged (data field `attackType` exists; hits are currently instant); deterministic tick-based projectile travel with speed from data; target shapes: single, hex radius r, line of n hexes, nearest-k; renderer events for spawn / travel / hit. — acceptance: a projectile hits on the predicted tick; radius and line selections tested on the hex grid; hashes identical with rendering off. — refs: infra
- [ ] (P0-03) [infra] Fight end rules — data-driven maxSeconds (exists), overtime hook expressed as effects (e.g. all units lose x% max hp per second), draw resolution rule (draw currently costs hp as a loss via `combat.drawCountsAsLoss`), FightResult with per-unit damage dealt / taken / healed, survivors, ticks, event count (all exist; verify against the final rules). — acceptance: mirrored boards draw at timeout; enabling the overtime effect ends the fight earlier; ledger totals equal the sum of hit events. — refs: infra
- [ ] (P0-04) [infra] Bot policy framework — policies are modules choosing from legalCommands(state) (interface + `random` exist in tools/policies/, documented in README); add greedy (buy the strongest affordable, place strongest first, level when the data-defined economy rule says so, merge whenever possible); sweep runs any policy by name (exists). — acceptance: 20-seed sweep runs both policies with zero exceptions; greedy beats random on mean rounds survived over 50 seeds. — refs: infra
- [ ] (P0-05) [infra] Save/load — versioned JSON save of run state + seed + command log; migration registry; corrupt-save repair falls back to the last good round boundary; dev "export run" button. — acceptance: round-trip test; corrupt-file test; migration test with a bumped version. — refs: infra
- [ ] (P0-06) [infra] Replay viewer — dev screen loads seed + command log and plays it back at 1/2/4× through the renderer (`replayRun` / `verifyReplay` and the fight timeline player exist), checking the state hash at every round boundary; mismatch displayed. — acceptance: a random-policy run replays with every round hash matching; a tampered log is flagged. — refs: infra
- [ ] (P0-07) [infra] Tuner v0 — dev-only panel: every /data file listed; edit → zod validate → save through the dev endpoint (`POST /__data/<path>` and `src/dev/dataClient.ts` exist) → content reload (next run uses it); invalid edits rejected with the schema error; content hash shown. Production build has neither panel nor endpoint (grep test exists in tests/slow/build.test.ts; extend it). — acceptance: edit→save→reload round-trip test via the endpoint; grep test proves dist/ contains no endpoint path. — refs: infra
- [ ] (P0-08) [infra] Codex v0 — read-only in-game browser generated from /data + schemas: every unit and encounter (later trait / item / augment) entry with its effects rendered as text. — acceptance: every data entry appears; snapshot test over dev content. — refs: infra
- [ ] (P0-10) [infra] Perf harness — `npm run bench` reports fights/sec and ticks/sec plus a host-independent budget (ticks per unit of a baseline micro-benchmark) (all printed today); numbers recorded in PROGRESS.md; slow-tier regression test at ±30% of the recorded budget. — acceptance: bench runs; budget recorded; test present in tests/slow. — refs: infra
- [ ] (P0-11) [infra] Fuzz — random legal + illegal command sequences: invariants hold (gold ≥ 0, board within bounds and cap, no duplicate uids, hp ≥ 0; `checkInvariants` exists), illegal commands rejected with reasons and no state change. Fast: 200 sequences × 200 steps; slow: 20k sequences. — acceptance: both tests pass. — refs: infra
- [ ] (P0-12) [infra] Soak — 200 random-policy runs to completion with zero exceptions / NaN / Infinity; slowest run and mean run time reported. tests/slow. — acceptance: passes. — refs: infra
- [ ] (P0-13) [infra] Telemetry — every run writes a report JSON (seed, commands, per-round hashes, outcome, timings; `buildRunReport` exists) to telemetry/ (gitignored) via the dev endpoint (dev) or a download button (prod); `tools/sweep.ts --telemetry <dir>` ingests human runs as extra seeds. — acceptance: a dev run produces a file; sweep ingests it. — refs: infra
- [ ] (P0-14) [infra] Lane readiness — BACKLOG-QUALITY.md template (hard scope: tests/**, tools/**, bench/**), OPS.md lane section verified against reality, SIEGE_TEST_WORKERS respected by vitest, and the sweep's own `SIEGE_SWEEP_WORKERS` documented as the separate knob it is (QA on P0-B1: the sweep does not read SIEGE_TEST_WORKERS; decide which name wins and make the code and this text agree), measured `npm run test:fast` time recorded (< 5 min; bootstrap measured ~2 s). — acceptance: template committed; time recorded; two loops on different worktrees run test:fast without exceeding 8 workers total. — refs: infra

### 4. Deferred QA bugs (end of P0)

Tooling ergonomics, not gameplay or gate defects — deferred by the owner triage verdict. At most one may be pulled forward per task, and only when it blocks the item being built (CLAUDE.md "Subagent protocol").

- [ ] (P0-B4) [bug] (deferred) CLI arg parsing accepts typos silently (QA on P0-B2) — a bare value-flag falls back to the default (`--out --policies random` writes to the default path; `--policies --workers 2` runs the default policy) because `flagString` returns the fallback when the value is `true`, while `flagInt` errors; unknown flags and stray positionals are ignored entirely, so `--seed 5` (singular) on `tools/sweep.ts` silently sweeps the default 20 seeds. Also `tools/fight.ts` and `tools/sweep.ts` quote unknown-policy names differently. — acceptance: failing tests first; `flagString` rejects a valueless flag with a UsageError; a `rejectUnknownFlags(args, allowed)` helper is applied by `fight`, `sim`, `sweep` and `bench` and tested for one unknown flag and one stray positional per tool; the unknown-policy message is identical across tools. — refs: infra (QA on P0-B2)
- [ ] (P0-B5) [bug] (deferred) Sweep has no watchdog and its CLI failure path is untested (QA on P0-B2) — a worker that takes a job and never answers hangs `runSweep` forever (not reachable with the shipped worker, which caps ticks and commands, but nothing enforces that); `--out` onto a FIFO blocks in the pre-flight `openSync`; the pre-flight also leaves a 0-byte report behind when a later step fails; and no test covers the user-visible CLI outcome of a crashed worker (report still written, exit code 1, every job present exactly once). — acceptance: an optional `jobTimeoutMs` on `runSweep` marks a stuck job `ok:false` and the sweep finishes, proved with a silent-worker fixture; the pre-flight refuses a non-regular-file `--out` instead of blocking; a `spawnSync` CLI test with an injectable worker entry point asserts exit 1, a parseable report and one result per job. — refs: infra (QA on P0-B2)
- [ ] (P0-B7) [bug] (deferred) `dev:spawnUnit` is stricter than `buy` on a full bench (QA on P0-15) — `buy` allows a purchase onto a full bench when a merge is guaranteed (`commands.ts` `wouldMerge`), while `dev:spawnUnit` rejects with `bench is full`, so the cheat cannot do what the shop can. — acceptance: a failing test first (two `dev.mage` on the bench, the other slots full, spawning a third `dev.mage` succeeds and merges to 2-star); `dev:spawnUnit` shares the full-bench-with-guaranteed-merge rule with `buy`, or QUESTIONS.md records the divergence as deliberate. — refs: owner-req (QA on P0-15)
- [ ] (P0-B10) [polish] (deferred) `InspectorStat.sources` aliases the caller-supplied `Modifier` objects (QA on P0-19) — `inspectorModel` (`src/app/inspectorModel.ts`) copies `items` (fixed in the same item after code review) but `sources: modifiers.filter(...)` keeps the *same* `Modifier` object references from the caller's `CombatOverlay.modifiers`, so a consumer mutating a returned `sources[i]` would silently corrupt whatever live modifier list the caller read it from. Currently unreachable (`inspectorModel` has no caller yet) so it cannot corrupt sim state or determinism today. — acceptance: `sources` entries are deep copies (`sources.map((m) => ({ ...m }))`), with a regression test asserting a mutated returned source does not affect the object the caller passed in. — refs: infra (QA on P0-19)
- [ ] (P0-B9) [polish] (deferred) Sandbox stat-override row rebuilds on every edit, breaking Tab navigation (QA on P0-18) — `renderSide` in `src/ui/sandboxUi.ts` calls `container.replaceChildren(...)` on every `update()`, including the one each stat/star/items edit triggers; that lands during the browser's Tab-driven focus-advance often enough that focus is thrown back to the row's star input, so keyboard-tabbing through a unit's 9 stat fields silently misdirects digits into the wrong field roughly every other Tab. Mouse-driven edits (click a field, type, click the next) are unaffected. — acceptance: a DOM-level test drives Tab-sequenced edits across all 9 stat fields on one unit and asserts all 9 values land in their own field; fixed by patching only the changed input's value/attributes instead of rebuilding the row, or by restoring focus after a rebuild. — refs: infra (QA on P0-18)
- [ ] (P0-B8) [polish] (deferred) `kindForPath` mis-routes a degenerate sandbox filename (QA on P0-17) — `dev/boards/sandbox-.json` (empty name suffix) fails the `sandboxSetupFile` pattern (`sandbox-[a-z0-9_-]+`, requires >= 1 char after the prefix) and falls through to the generic `boardFile` pattern instead of `null`, so a file with that exact name would be silently validated against the wrong schema by the future dev data endpoint. No real dev-authored name hits this. — acceptance: `kindForPath('dev/boards/sandbox-.json')` returns `null` (neither schema fits an empty name), with a regression test. — refs: infra (QA on P0-17)
- [ ] (P0-B6) [bug] (deferred) `--maxWorkers=-5` still runs zero tests and exits 0 (QA on P0-B3) — P0-B3 closed the env-var door, but the same "green having run nothing" signature is reachable through the flag: `npm run test:fast -- --maxWorkers=-5` prints no "Test Files" line and exits 0 (vitest validates `--minWorkers` but not `--maxWorkers`). The per-item gate must not be silenceable by a flag either. Also `run-until-done.ps1` passes `-TestWorkers` through as a bare `[int]`, so an out-of-range value now hard-fails every loop iteration. — acceptance: a failing regression test first; a spawnSync test proves `vitest run --maxWorkers=-5 tests/hex.test.ts` either exits non-zero or collects test files, with the guard implemented in `vitest.config.ts` (normalise the resolved pool options) rather than in the npm scripts; `run-until-done.ps1` validates `-TestWorkers` to 1..64 before the loop starts. — refs: infra (QA on P0-B3)
- [ ] (P0-B13) [polish] (deferred) `summaryLines`' "the run ends here" branch is unreachable (QA on P0-21) — `resolveCombat` (`src/sim/run.ts`) never enters the `reward` phase for the run's final round (it calls `endRun` directly instead), so `src/ui/runUi.ts`'s `summaryLines` can never actually be called with `state.round === content.encounters.length` in the `reward` phase; its "the run ends here" fallback string never renders in practice. Not incorrect output, just dead/untested code. — acceptance: either delete the unreachable branch (and the `content.encounters[state.round] ?? null` null-check that guards it) with a comment explaining why the reward phase can't reach it, or add a regression test constructing a synthetic `RunState` by hand (since driving it through commands cannot reach this state) proving the fallback text is correct if it were ever reached. — refs: infra (QA on P0-21)
- [ ] (P0-B14) [polish] (deferred) Duplicate CSS/query classes make Playwright-style DOM queries ambiguous (QA on P0-21) — `run-ui` is shared by the actual run screen root (`src/ui/runUi.ts`) and the always-present dev-fight bar (`src/app/main.ts`), and `hud` is shared by the run HUD and the sandbox HUD; both pairs are correctly `hidden` on inactive screens so there is no visible or functional collision today, but any future DOM-driven test (Playwright or otherwise) querying by class alone gets multiple matches. — acceptance: rename the dev-fight bar's class (e.g. `dev-fight-bar`) and the sandbox HUD's class (e.g. `sandbox-hud`), or scope future DOM queries to a visible/non-hidden root; no behavior change, so `npm run test:fast` staying green is the whole check. — refs: infra (QA on P0-21)
- [ ] (P0-B15) [polish] (deferred) An intermittent `404` console error in `npm run dev` (QA on P0-21) — observed inconsistently across manual/Playwright sessions, not tied to any P0-21 code path (likely a missing favicon or similar static asset); no functional impact observed in any tested scenario. — acceptance: identify the missing resource from the dev server's network log and either add it or confirm it is browser-requested boilerplate (e.g. `favicon.ico`) that can be silenced with a real favicon file. — refs: infra (QA on P0-21)
